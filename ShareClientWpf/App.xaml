<Application
    x:Class="ShareClientWpf.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ShareClientWpf"
    StartupUri="View\MainWindow.xaml">
    <Application.Resources>

        <Brush x:Key="WindowBackGround">#FF333333</Brush>
        <Brush x:Key="HeaderBackGround">#202020</Brush>
        <Brush x:Key="HeaderForeGround">LightGray</Brush>
        <Brush x:Key="ButtonBackGround">#5F5E5E</Brush>
        <Brush x:Key="ButtonMouseOver">#7C7C7C</Brush>
        <Brush x:Key="ButtonForeGround">LightGray</Brush>
        <Brush x:Key="DialogForeGround">LightGray</Brush>

        <Style x:Key="DefaultWindowStyle" TargetType="{x:Type Window}">
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome
                        CaptionHeight="0"
                        CornerRadius="2"
                        GlassFrameThickness="0"
                        ResizeBorderThickness="5"
                        UseAeroCaptionButtons="True" />
                </Setter.Value>
            </Setter>
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource WindowBackGround}" />
            <Setter Property="Foreground" Value="Silver" />
        </Style>

        <ControlTemplate TargetType="local:WindowBase" x:Key="DailogTemplate">

            <Border Padding="1" Background="{TemplateBinding ThemeBrush}">

                <Grid Background="{TemplateBinding Background}">
                
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource HeaderBackGround}">

                        <TextBlock x:Name="TitleTextBlock"
                                   Margin="12,4,12,2"
                                   FontSize="16"
                                   Text="{TemplateBinding Title}" />
                    </Border>

                <ContentPresenter Grid.Row="1" 
                                  HorizontalAlignment="Stretch" 
                                  VerticalAlignment="Stretch"/>

            </Grid>

            </Border>
        </ControlTemplate>

        <Style
            x:Key="DialogWindowStyle"
            BasedOn="{StaticResource DefaultWindowStyle}"
            TargetType="{x:Type local:WindowBase}">
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome
                        CaptionHeight="30"
                        CornerRadius="2"
                        GlassFrameThickness="0"
                        ResizeBorderThickness="0"
                        UseAeroCaptionButtons="True" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template" Value="{StaticResource DailogTemplate }"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="{StaticResource WindowBackGround}"/>
            <Setter Property="Foreground" Value="{StaticResource DialogForeGround}"/>
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="Width" Value="500"/>
            <Setter Property="Height" Value="350"/>
        </Style>

        <ControlTemplate x:Key="DefaultButtonTemplate" TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Background}">
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    Foreground="{TemplateBinding Foreground}"
                    Text="{TemplateBinding Content}" />
            </Border>
        </ControlTemplate>

        <Style x:Key="DefaultButtonStyle" TargetType="Button">
            <Setter Property="Template" Value="{StaticResource DefaultButtonTemplate}" />
            <Setter Property="Background" Value="{StaticResource ButtonBackGround}" />
            <Setter Property="Foreground" Value="{StaticResource ButtonForeGround}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ButtonMouseOver}" />
                </Trigger>
            </Style.Triggers>
        </Style>

    </Application.Resources>
</Application>
